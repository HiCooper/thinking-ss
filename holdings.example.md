# 持仓记录（模板）

> **这是模板文件，随仓库分发；你的真实持仓请写进同目录的 `holdings.md`。**
>
> `holdings.md` 与它生成的 `market-dashboard/public/holdings.json` 都已被 `.gitignore` 忽略
> （处理方式同 `.env`），**不会进仓库**。所以这个模板里全是编造的假数据，照抄后请全部替换。
>
> ```bash
> cp holdings.example.md holdings.md          # 在仓库根目录执行
> # 编辑 holdings.md，填入自己的持仓
> cd market-dashboard && npm run holdings:export
> ```
>
> 字段口径、校验规则与「怎么查代码 / 怎么抓现价」见 `AGENTS.md` §B2–§B5。

> **快照时间**：2026-01-01 15:00
> **账户**：示例账户

---

## 一、总览

这一节是给人看的，脚本不读，可以随便写。

| 项目 | 数值 |
| --- | --- |
| 持仓条数 | 3 |
| 持仓市值 | 37,000.00 元 |
| 浮动盈亏 | −2,600.00 元 |

---

## 二、明细（按类型分组，组内按市值降序）

### A. 科技成长

| 名称 | 份额 | 现价 | 成本 | 市值 | 盈亏 | 盈亏% |
| --- | --- | --- | --- | --- | --- | --- |
| 科创50ETF华夏 | 10,000 | 1.200 | 1.000 | 12,000.00 | 2,000.00 | +20.00% |
| 半导体设备ETF国泰 | 20,000 | 0.800 | 1.000 | 16,000.00 | −4,000.00 | −20.00% |

### B. 宽基

| 名称 | 份额 | 现价 | 成本 | 市值 | 盈亏 | 盈亏% |
| --- | --- | --- | --- | --- | --- | --- |
| 沪深300ETF华泰柏瑞 | 2,000 | 4.500 | 4.800 | 9,000.00 | −600.00 | −6.25% |

---

## 三、代码映射

| 名称 | 已确认全称 | 代码 |
| --- | --- | --- |
| 科创50ETF华夏 | 科创50ETF华夏 | 588000 |
| 半导体设备ETF国泰 | 半导体设备ETF国泰 | 159516 |
| 沪深300ETF华泰柏瑞 | 沪深300ETF华泰柏瑞 | 510300 |

---

## 四、如何填（要点速查，填完可删）

1. **分组标题**必须写成 `### 单个大写字母. 组名`（如 `### A. 科技成长`）。
   分成 2–5 组，按主题或资产类别分；持仓看板「图 1 分组结构」直接依赖分组。
2. **明细表固定 7 列**：`名称 | 份额 | 现价 | 成本 | 市值 | 盈亏 | 盈亏%`。
   只有列序有要求，**列名不校验**（叫 `浮亏` 或 `盈亏` 都能解析）。
3. **`市值` 必须等于 `份额 × 现价`**（容差 0.5%）—— 脚本靠这条校验有没有读错列。
4. **`成本` 与 `盈亏%` 两列脚本不读**，填个大概即可：脚本用
   `成本 = (市值 − 盈亏) / 份额` 反推精确成本价（行情软件显示的 3 位小数是四舍五入值）。
5. **`盈亏` 可正可负**：盈利填正数、亏损填负数，全角 `−` 与 ASCII `-` 都认。
6. **代码映射表**：6 位代码，第 1 列要与明细表的名称一致（用全称匹配也可以）。
   查代码的零依赖办法见 `AGENTS.md` §B3（新浪 suggest 接口），**不要猜代码**。
7. 每条持仓都要在映射表里找得到代码，且**代码不能重复** —— 否则脚本报错并列明缺哪几只。
